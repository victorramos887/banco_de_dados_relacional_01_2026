# Atividade Prática - Aula 4 (BDR)

Entrega em `.sql` conforme combinado.

## Arquivos

- `schema.sql`:
  - Cria o banco `clima_alerta`.
  - Cria as tabelas: `TipoEvento`, `Localizacao`, `Usuario`, `Evento`, `Relato`, `Alerta`.
  - Cria a tabela auxiliar: `HistoricoEvento`.
  - Inclui `INSERT` de exemplo com os valores do enunciado (IDs 1, 2 e 5).

## Observações

- Script em sintaxe MySQL 8+ (InnoDB / utf8mb4).
- Não é necessário testar/rodar para esta entrega.
